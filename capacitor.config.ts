import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'vue3.vite.ts.template',
  appName: 'vue3-vite-ts-template',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
